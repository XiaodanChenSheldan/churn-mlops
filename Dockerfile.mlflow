# Dockerfile.mlflow
FROM python:3.10-slim

RUN pip install mlflow>=3.0 psycopg2-binary boto3

EXPOSE 5000

# Use --allowed-hosts flag for MLflow 3.x
CMD ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--allowed-hosts", "*"]